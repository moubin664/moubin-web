# 部署说明

这是一个纯静态网站，无需 Node.js、MySQL 或数据库配置。

## 宝塔 / Nginx

1. 在宝塔创建一个静态网站，网站根目录指向项目目录。
2. 默认文档设置为 `index.html`。
3. 申请 SSL 证书并开启 HTTPS。

更新网站文件后，浏览器刷新即可。网站内容集中维护在项目根目录的 `script.js`：向 `articles` 数组新增一项，文章会自动进入模块索引和搜索结果。
